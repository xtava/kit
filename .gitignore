/target

# Only canonical documentation belongs in the public repo
/CONTEXT.md
/plans/
/docs/*
!/docs/canonical/
!/docs/canonical/**

# kit deploy — project-local config holds paths, account IDs, and secret-file refs; never commit it
.kit/deploy.toml
.kit/*.env
