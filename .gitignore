/target

# Internal docs — not for the public repo
/CONTEXT.md
/docs/plan.md
/plans/
/docs/adr/

# kit deploy — project-local config holds paths, account IDs, and secret-file refs; never commit it
.kit/deploy.toml
.kit/*.env
